This sentence uses *emphasis syntax*.

This sentence uses **strong syntax**.

This sentence uses ``literal (raw-text)``.

.. role:: python(code)
   :language: python

This sentence uses :python:`literal(raw_text)`.
